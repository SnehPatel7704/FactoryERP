import React from 'react'
import { describe, it, expect } from 'vitest'
import ScanQR from './ScanQR'

describe('ScanQR', () => {
  it('is a function component', () => {
    expect(typeof ScanQR).toBe('function')
  })
})
